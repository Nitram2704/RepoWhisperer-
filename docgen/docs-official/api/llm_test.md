```markdown
## API Documentation for `llm_test` module

### `test_prompt_formatting` function
```python
def test_prompt_formatting():
```

This function appears to test the formatting of a user prompt. It defines a list of code chunks and a skeleton project structure. It then calls `format_user_prompt` and asserts that certain strings are present in the resulting prompt. It's designed to verify the correct construction of the prompt string used for the LLM.
```